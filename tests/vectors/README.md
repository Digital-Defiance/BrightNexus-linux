# Known-answer vectors (RFC §13)
#
# Transcript, HKDF, and DD-ECIES vectors are cross-validated against
# libbrightlink (`tests/mock-brightnexus`) and bsh `test-harness`.
#
# Run: cargo test -p brightnexus-core session::tests
